# Copy this file to .env and paste your key below.
#   cp .env.example .env
#
# .env is git-ignored and is never read, written or printed by anything
# but the key loader. Use a dedicated project-scoped key for this job and
# revoke it once the invoice backlog is cleared.

OPENAI_API_KEY=
